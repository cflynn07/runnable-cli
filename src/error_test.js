/**
 * @module lib/error_test
 */
'use strict'

const test = require('unit.js')

const error = require('./error')

describe('src/error.js', () => {
  it('load', () => {
    test.value(error).isFunction
  })

  describe('export function', () => {
    beforeEach(() => {
    })

    afterEach(() => {
    })

    it('', () => {
    })
  })
})
